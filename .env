# Enterprise LLM API Platform Configuration
# ================================================

# Google API Configuration (REQUIRED)
GOOGLE_API_KEY=your_google_api_key_here

# Server Configuration (HARDCODED IN app.py)
# Note: These values are hardcoded in the application for deployment consistency
# HOST=0.0.0.0  # Hardcoded - listens on all interfaces
# PORT=8081     # Hardcoded - application port

# Application Settings (Optional)
FLASK_ENV=production
DEBUG=false

# Logging Configuration (Optional)
LOG_LEVEL=INFO
LOG_DIR=logs

# Cache Configuration (Optional - defaults in code)
CACHE_MAX_SIZE=500
CACHE_DEFAULT_TTL=1800

# Rate Limiting (Optional - defaults in code)
RATE_LIMIT_CACHE_SIZE=10000
RATE_LIMIT_PER_MINUTE=10
RATE_LIMIT_PER_HOUR=50
RATE_LIMIT_PER_DAY=200

# Cost Tracking (Optional)
COST_LOG_FILE=logs/cost_tracking.log
COST_CLEANUP_DAYS=30

# Security Settings (Optional)
CORS_ORIGINS=*
SECURITY_LOG_LEVEL=WARNING

# Production Deployment Notes:
# - The application is hardcoded to run on 0.0.0.0:8081
# - Swagger UI will be available at: http://0.0.0.0:8081/swagger/
# - Health check endpoint: http://0.0.0.0:8081/api/v1/health
# - Stats endpoint: http://0.0.0.0:8081/api/v1/stats
